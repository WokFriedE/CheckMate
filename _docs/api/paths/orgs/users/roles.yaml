post:
  summary: Assign organizer role
  parameters:
    - name: org_id
      in: path
      required: true
      schema:
        type: string
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../../components/schemas/RoleAssignmentDetailed.yaml"
  responses:
    "201":
      description: Role assigned
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/Message.yaml"
          example:
            message: Example response for 201
    "400":
      description: Invalid request
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/Error.yaml"
          example:
            message: Example response for 400
    "500":
      description: Server error
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/Error.yaml"
          example:
            message: Example response for 500
  tags:
    - Multi-tasked Management
  operationId: _orgs_org_id_users_roles_post
  description: POST operation for /orgs/{org_id}/users/roles.

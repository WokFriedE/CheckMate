get:
  summary: List notifications
  parameters:
    - name: org_id
      in: path
      required: true
      schema:
        type: string
  responses:
    "200":
      description: Notifications
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../components/schemas/Notification.yaml"
          example:
            message: Example response for 200
  tags:
    - Handling Order Requests
  operationId: _orgs_org_id_notifications_get
  description: GET operation for /orgs/{org_id}/notifications.
post:
  summary: Create a notification
  parameters:
    - name: org_id
      in: path
      required: true
      schema:
        type: string
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/NotificationCreate.yaml"
  responses:
    "201":
      description: Created
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/Message.yaml"
          example:
            message: Example response for 201
  tags:
    - Handling Order Requests
  operationId: _orgs_org_id_notifications_post
  description: POST operation for /orgs/{org_id}/notifications.

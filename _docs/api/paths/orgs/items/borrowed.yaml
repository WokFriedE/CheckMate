get:
  summary: List borrowed items with optional filters
  parameters:
    - name: org_id
      in: path
      required: true
      schema:
        type: string
    - name: filter_by
      in: query
      required: false
      schema:
        type: string
        enum:
          - organization_id
          - event_id
  responses:
    "200":
      description: Borrowed items
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../../components/schemas/BorrowedItem.yaml"
          example:
            message: Example response for 200
  tags:
    - Multi-tasked Management
  operationId: _orgs_org_id_items_borrowed_get
  description: GET operation for /orgs/{org_id}/items/borrowed.

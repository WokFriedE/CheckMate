get:
  summary: Get a request details
  parameters:
    - name: org_id
      in: path
      required: true
      schema:
        type: string
    - name: order_id
      in: path
      required: true
      schema:
        type: string
  responses:
    "200":
      description: Request
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/OrderRequest.yaml"
          example:
            message: Example response for 200
  tags:
    - Handling Order Requests
  operationId: _orgs_org_id_item_request_order_id_get
  description: GET operation for /orgs/{org_id}/item/request/{order_id}.
post:
  summary: Approve or deny a request
  parameters:
    - name: org_id
      in: path
      required: true
      schema:
        type: string
    - name: order_id
      in: path
      required: true
      schema:
        type: string
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../../components/schemas/OrderDecision.yaml"
  responses:
    "200":
      description: Updated
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/Message.yaml"
          example:
            message: Example response for 200
  tags:
    - Handling Order Requests
  operationId: _orgs_org_id_item_request_order_id_post
  description: POST operation for /orgs/{org_id}/item/request/{order_id}.
